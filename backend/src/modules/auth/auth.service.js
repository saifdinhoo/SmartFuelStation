// Business logic for authentication will live here (register, login, etc.).
// Left empty for now — implemented in a later task.

async function register(userData) {
  throw new Error('Not implemented yet');
}

async function login(credentials) {
  throw new Error('Not implemented yet');
}

module.exports = {
  register,
  login,
};
